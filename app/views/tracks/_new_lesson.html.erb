
<h2>Add a Lesson</h2>
<% binding.pry %>
<%= form_for ([:track, @lessons]), remote: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :duration %>
  <%= f.text_field :duration %>
  <%= f.hidden_field :track_id %>
  <%= f.submit %>
  <% end %>
