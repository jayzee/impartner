<h1> <%= @category.title %> </h1>
<ul>
  <% @tracks.each do |track| %>
    <li><%= link_to track.name, track_path(track) %> - <%= track.description %></li> 
  <% end %>

</ul>

  <%= link_to "Create a New Track", new_category_track_path(@category.id) %>